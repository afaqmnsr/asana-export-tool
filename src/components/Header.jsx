import React from 'react';

const Header = () => {
  return (
    <header className="app-header">
      <h1>Asana Data Exporter</h1>
      <p>Export all your tasks, subtasks, and comments across every workspace</p>
    </header>
  );
};

export default Header;
